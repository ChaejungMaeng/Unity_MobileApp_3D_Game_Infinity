using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using UnityEngine.UI;

public class speed : MonoBehaviour
{
   /* private Image boost;

    private void Awake()
    {
        boost = GetComponent<Image>();
    }

    private void Update()
    {
        if (boost != null && ShipInput.PlayerShip != null)
        {
            boost.enabled = Ship.PlayerShip.UsingMouseInput;

            if (boost.enabled)
            {
                boost.transform.position = Input.mousePosition;
                Cursor.visible = false;
                Cursor.lockState = CursorLockMode.Confined;
            }
            else
            {
                Cursor.visible = true;
                Cursor.lockState = CursorLockMode.None;
            }
        }
    }*/
}
